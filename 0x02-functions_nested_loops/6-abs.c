/**
 * _abs - Computes the absolute value
 * of an interger.
 *
 * @i: input number as an interger.
 *
 * Return: absolute value
 */
#include "main.h"
int _abs(int q)
{
	if (i >= 0)
{
	return(i);
}
	else
{
	return(i * -1);
}
}
