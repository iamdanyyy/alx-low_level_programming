#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @k: integer to be computed
 * Return: Absolute value of k
 */

int _abs(int k)
{

	if (k <= -1)
	{
		return (-k);
	}

	return (k);

}
