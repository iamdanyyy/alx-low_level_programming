#include <stdio.h>

/**
 * main - prints the alphabet in lowercase, followed by a new line
 * Return: Always 0
 */

int main(void) {

	char alpha[] = "abcdefghijklmnopqrstuvwxyz";
	
	putchar("%c\n", alpha);

	return (0);

}
