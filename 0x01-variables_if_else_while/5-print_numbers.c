#include <stdio.h>

/**
 * main prints single digit numbers of base ten starting from zero,then a new line
 * Return: Always 0
 */

int main(void)
{

	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}

	printf("\n");

	return (0);

}
