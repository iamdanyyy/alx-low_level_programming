#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: string as parameter
 * Return: Length of string
 */

int _strlen(char *s)
{
	int length;

	length = strleni(s);

	return (length);
}
